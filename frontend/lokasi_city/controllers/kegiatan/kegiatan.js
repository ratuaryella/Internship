const fetchKegiatan = require('../../fetch/kegiatan/kegiatan');

const getAllKegiatan = async (req, res, next) => {
  // return await fetchKegiatan.getAllKegiatan(req)
  // .then()
}

module.exports = {
  getAllKegiatan,
}